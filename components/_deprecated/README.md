# Archivierte Komponenten

Hier liegen UI-Teile, die **nicht mehr** von der aktuellen Lektionsseite importiert werden, aber für spätere Wiederverwendung (z. B. Offline-Folien) im Repo bleiben.

| Datei | Kontext |
|--------|---------|
| `SlidesViewer.tsx` | Folien-Hauptansicht |
| `SlideNavigator.tsx` | Vor/Zurück |
| `SlideThumbnailStrip.tsx` | Thumbnail-Leiste |

**Stand:** Phase 1 UX (Key-Takeaways-Vorbereitung) — Folien laufen nur noch im Video; `slides.json` unverändert für die Video-Pipeline.
